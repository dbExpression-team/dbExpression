    #region runtime environment db
    {?var:dbName = ($) => ResolveName}
    public class {:dbName} : RuntimeEnvironmentSqlDatabase
    {{
        public {:dbName}() : base(new db(), new SqlDatabaseMetadataProvider(new {:dbName}SqlDatabaseMetadata("{:dbName}", "{($) => ResolveStrictName}")))
        {{ }}
    }}
    #endregion