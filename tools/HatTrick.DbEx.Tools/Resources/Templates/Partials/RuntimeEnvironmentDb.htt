    #region runtime environment db
    {?var:dbName = $.DatabaseExpression.Name}
    public class {:dbName} : RuntimeEnvironmentSqlDatabase
    {{
        public {:dbName}() : base(new db(), new SqlDatabaseMetadataProvider(new {:dbName}SqlDatabaseMetadata("{:dbName}", "{$.Database.Name}")))
        {{ }}
    }}
    #endregion