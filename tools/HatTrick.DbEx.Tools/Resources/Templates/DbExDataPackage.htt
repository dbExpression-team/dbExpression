using System;
using HatTrick.DbEx.Sql;

{#each Schemas.Values}
{#if !($) => IsIgnored}
namespace {() => ResolveRootNamespace}{($) => ResolveName}Data
{{
	{#each ($) => ResolveConsolidatedTablesAndViews}
	{#if !($) => IsIgnored}
{>("DataPackage") => GetTemplatePartial+}
	{/if}
	{/each}
}}
{/if}
{/each}
