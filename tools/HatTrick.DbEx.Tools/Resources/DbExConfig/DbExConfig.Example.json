{
	"source": {
		"type": "MsSqlDb",
		"connectionString": {
			"value": "** connection string for target db **"
		}
	},
	"workingDirectory": "./", /* optional: overrides default working dir of execuation relative to THIS file.  so ./ is the path this file resides in */
	"outputDirectory": "./Generated", /* optional: generated code output path, if not provided defaults to workingDirectory/DbExGenerated */
	"enums": [ /* optional: provided a list of known or used enumeration types */
		"AddressType"
	],
	"overrides": [
		/* ** examples below **
       	{
			"apply": {
				"ignore": true,
				"to": { "path": "dbo.sysdiagrams" }
			}
		},
		{
			"apply": {
				"ignore": true,
				"to": { "path": "sec.sysdiagrams" }

			}
		},
		{
			"apply": {
				"name": "PersonAddress",
				"to": {
					"path": "dbo.Person_Address"
				}
			}
		},
		{
			"apply": {
				"clrType": "AddressType?",
				"to": {
					"path": "dbo.Address.AddressType"
				}
			}
		}
*/
	]
}
